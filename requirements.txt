# for host side, to install packages needed to interpret recorded bag files
rospy # python 3.8 and older; https://github.com/rospypi/simple for windows without ROS, just to have packages to interpret messages
pigpio
numpy
libnmea_navsat_driver
functools
smbus
pathlib

# Orange Pi hardware interface libraries (for onboard use)
spidev  # SPI communication for LoRa SX1276/SX1278 module (nodes/lora.py)
OPi.GPIO  # GPIO control for LoRa control pins (CS/RST/IRQ) and other GPIO operations (nodes/lora.py)