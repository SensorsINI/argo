bno08x_ros:
  ros__parameters:

    frame_id: "bno085"  # The frame_id to use for the sensor data

    # Communication Interface
    # Select the communication interface to use 
    # (only one interface can be enabled at a time)
    # This requires a hardware change to the board
    # (see documentation for more information)
    i2c:
      enabled: true
      bus: "/dev/i2c-0"
      address: "0x4A"

    publish:
      magnetic_field: 
        enabled: true
        rate: 1     # 1 Hz for magnetic field (compass heading)
      imu:
        enabled: true
        rate: 20    # 20 Hz fusion (was 5) — responsive compass/verify; restart argo_bno085.service
